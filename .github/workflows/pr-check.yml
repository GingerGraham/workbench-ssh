name: PR Check

on:
  pull_request:
    branches: [main]

jobs:
  commit-format:
    # TODO: bump to @main and drop core_ref once GingerGraham/workbench-core#33 (D40) merges.
    uses: GingerGraham/workbench-core/.github/workflows/module-pr-check.yml@d926f15a055dccb759cdda545c3c2fc75c097968
    with:
      core_ref: d926f15a055dccb759cdda545c3c2fc75c097968
